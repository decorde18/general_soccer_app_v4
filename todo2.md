We have made a lot of changes, how are we sure we are not placing a lot of band-aids on issues and make sure that any fixes are permanent.

we need verify that all are using same components........
we need to make things like a location universal where it can be clicked for a modal whether in a table or card or whatever. Then it should also be able to use the abbreviation if a small field and longer name in the modal (or possibly if the table is based on the location, then the full name will be used)
For location, look how it is done in the schedule view of a team. That should be the default for clicking on a location. This same procedure should be implemented for all things (ie clubs,)
